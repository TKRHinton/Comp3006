npm init
npm install express
npm install save mongoose
npm install mongodb
npm install ejs
npm install nodemon -g
npm install bcrypt
npm install express-session

cd C:\Users\Theon\Documents\GitHub\Comp3006\Project
nodemon REVW-app.js
node REVW-app.js


show databases
use revwdb
db.users.find({})
db.users,insertOne({userName: "Thomas", userPassword: "1231 })


let gameSchema = new mongoose.Schema({

    gameID: String,
    gameName: String,
    gamePlatform: String,
    gameDecription: String,
    gameTotalScore: String,
    reviews: [
        {reviewID: String, gameID: String, userID: String, reviewScore: String, reviewDescription: String, reviewDate: String}
    ]
});
let Game = mongoose.model("game", gameSchema);
module.exports.Game = Game;

DI WD YT